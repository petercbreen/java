/**
 * 
 */
package controller;

/**
 * The commands that the game engine expects. NONE represents a noop and
 * indicates that the player did not take any action.
 * 
 * @author fennell
 */
public enum Command {
	LEFT, RIGHT, FIRE, NONE
}
